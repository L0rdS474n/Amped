// STUB: compile-only skeleton (M0 bootstrap exception, ADR-0001(a)).
// Tauri shell (kiosk, commands, IPC) is M3. NO behavioral logic here.
// amped-host is declared as a dependency to validate the crate graph compiles;
// it is not actually used until M3.
fn main() {
    // placeholder — real entry point added in M3
}
