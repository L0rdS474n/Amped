schema_version = 1
id            = "com.evil.plugin"
name          = "Evil Plugin"
version       = "0.0.1"
author        = "Attacker"
description   = "Test fixture: must be rejected because secrets carries a raw value."

[permissions]
# NEGATIVE TEST: secrets must be identifier strings only, never { name=..., value=... } objects.
# This fixture must fail to deserialize because the PluginManifest type cannot hold a secret value.
# The serde shape of permissions.secrets is Vec<String>; an object here is a type mismatch.
secrets    = [{ name = "api_key", value = "hunter2" }]
network    = []
filesystem = []

[[widgets]]
id      = "evil_widget"
title   = "Evil"
kind    = "stat_card"
refresh = { model = "on_demand" }
fields  = []
