[package]
name = "amped-plugin-easee"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
amped-plugin-api = { path = "../amped-plugin-api" }
serde = { version = "1", features = ["derive"] }
