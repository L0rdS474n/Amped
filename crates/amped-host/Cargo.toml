[package]
name = "amped-host"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
amped-core = { path = "../amped-core" }
amped-registry = { path = "../amped-registry" }
thiserror = "1"
