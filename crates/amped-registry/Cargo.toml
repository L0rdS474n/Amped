[package]
name = "amped-registry"
version = "0.1.0"
edition = "2021"

[dependencies]
amped-plugin-api = { path = "../amped-plugin-api" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
